services:
  frontend:
  build: .
  ports:
    - "5173:5173"
  volumes:
    - .:/app
    - /app/node_modules
  environment:
    - CHOKIDAR_USEPOLLING=true # Garante o hot reload em alguns sistemas
  studin_open: true
  tty: true